{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Home","text":"<p> <p>     Python wrapper for the SeaDex API   </p> </p> <p> </p> <p> </p>"},{"location":"#table-of-contents","title":"Table Of Contents","text":"<ul> <li>About</li> <li>Installation</li> <li>License</li> </ul>"},{"location":"#about","title":"About","text":"<p>Python wrapper for the SeaDex API.</p>"},{"location":"#installation","title":"Installation","text":"<p><code>seadex</code> is available on PyPI, and can be installed using pip.</p> <ol> <li> <p>To install the core library:</p> <pre><code>pip install seadex\n</code></pre> </li> <li> <p>To install with the CLI tools:</p> <ul> <li> <p>With <code>pipx</code> or <code>uv</code> (recommended)</p> <p><pre><code>pipx install \"seadex[cli]\"\n</code></pre> <pre><code>uv tool install \"seadex[cli]\"\n</code></pre></p> </li> <li> <p>With <code>pip</code></p> <pre><code>pip install \"seadex[cli]\"\n</code></pre> </li> </ul> </li> </ol>"},{"location":"#license","title":"License","text":"<p>Distributed under the MIT License. See LICENSE for more information.</p>"},{"location":"api-reference/backup/","title":"Backup","text":""},{"location":"api-reference/backup/#seadex.SeaDexBackup","title":"SeaDexBackup","text":"<pre><code>SeaDexBackup(email: str, password: str, base_url: str = 'https://releases.moe', client: Client | None = None)\n</code></pre> <p>Client to interact with the SeaDex backup API.</p> <p>Parameters:</p> Name Type Description Default <code>email</code> <code>str</code> <p>The email address for authentication.</p> required <code>password</code> <code>str</code> <p>The password for authentication.</p> required <code>base_url</code> <code>str</code> <p>The base URL of SeaDex, used for constructing API queries.</p> <code>'https://releases.moe'</code> <code>client</code> <code>Client</code> <p>An <code>httpx.Client</code> instance used to make requests to SeaDex.</p> <code>None</code> <p>Examples:</p> <pre><code>with SeaDexBackup(\"me@email.com\", \"password\") as seadex_backup:\n    print(seadex_backup.latest_backup)\n    #&gt; @auto_pb_backup_sea_dex_20241122000000.zip\n</code></pre> Notes <p>Only SeaDex admins can use this! Logging in with a non-admin account will result in failure.</p> Source code in <code>src/seadex/_backup.py</code> <pre><code>def __init__(\n    self, email: str, password: str, base_url: str = \"https://releases.moe\", client: Client | None = None\n) -&gt; None:\n    \"\"\"\n    Client to interact with the SeaDex backup API.\n\n    Parameters\n    ----------\n    email : str\n        The email address for authentication.\n    password : str\n        The password for authentication.\n    base_url : str, optional\n        The base URL of SeaDex, used for constructing API queries.\n    client : Client, optional\n        An [`httpx.Client`](https://www.python-httpx.org/api/#client) instance used to make requests to SeaDex.\n\n    Examples\n    --------\n    ```py\n    with SeaDexBackup(\"me@email.com\", \"password\") as seadex_backup:\n        print(seadex_backup.latest_backup)\n        #&gt; @auto_pb_backup_sea_dex_20241122000000.zip\n    ```\n\n    Notes\n    -----\n    Only SeaDex admins can use this! Logging in with a non-admin account will result in failure.\n\n    \"\"\"\n    self._base_url = base_url\n    self._client = httpx_client() if client is None else client\n    self._admin_token = self._auth_with_password(email, password)\n</code></pre>"},{"location":"api-reference/backup/#seadex.SeaDexBackup.backups","title":"backups  <code>property</code>","text":"<pre><code>backups: tuple[BackupFile, ...]\n</code></pre> <p>Retrieve a tuple of backup files.</p> <p>Returns:</p> Type Description <code>tuple[BackupFile, ...]</code> <p>A tuple of backup files, sorted by the modified date.</p>"},{"location":"api-reference/backup/#seadex.SeaDexBackup.base_url","title":"base_url  <code>property</code>","text":"<pre><code>base_url: str\n</code></pre> <p>Base URL, used for constructing API queries.</p>"},{"location":"api-reference/backup/#seadex.SeaDexBackup.latest_backup","title":"latest_backup  <code>property</code>","text":"<pre><code>latest_backup: BackupFile\n</code></pre> <p>Retrieve the latest backup file.</p> <p>Returns:</p> Type Description <code>BackupFile</code> <p>The latest backup file.</p>"},{"location":"api-reference/backup/#seadex.SeaDexBackup.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close the underlying HTTP client connection.</p> Source code in <code>src/seadex/_backup.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"\n    Close the underlying HTTP client connection.\n    \"\"\"\n    self._client.close()\n</code></pre>"},{"location":"api-reference/backup/#seadex.SeaDexBackup.create","title":"create","text":"<pre><code>create(filename: str) -&gt; BackupFile\n</code></pre> <p>Create a new backup with the specified filename.</p> <p>Parameters:</p> Name Type Description Default <code>filename</code> <code>str</code> <p>The name to assign to the backup file. This can include full formatting options as supported by <code>datetime.strftime</code>.</p> required <p>Returns:</p> Type Description <code>BackupFile</code> <p>The newly created backup file.</p> Source code in <code>src/seadex/_backup.py</code> <pre><code>def create(self, filename: str) -&gt; BackupFile:\n    \"\"\"\n    Create a new backup with the specified filename.\n\n    Parameters\n    ----------\n    filename : str\n        The name to assign to the backup file.\n        This can include full formatting options as supported by\n        [`datetime.strftime`][datetime.datetime.strftime].\n\n    Returns\n    -------\n    BackupFile\n        The newly created backup file.\n\n    \"\"\"\n    _filename = filename.removesuffix(\".zip\") + \".zip\"\n    _filename = datetime.now(timezone.utc).strftime(_filename).casefold()\n\n    if re.match(r\"^([a-z0-9_-]+\\.zip)$\", _filename) is None:\n        # The API forbids anything else, so we need to enforce it.\n        raise ValueError(\n            f\"Invalid filename: {_filename!r}. The filename may only contain alphanumeric characters, hyphens, or underscores.\"\n        )\n\n    self._client.post(\n        self._url_for(\"/api/backups\"), json={\"name\": _filename}, headers={\"Authorization\": self._admin_token}\n    ).raise_for_status()\n\n    return next(filter(lambda member: member.name == _filename, self.backups))\n</code></pre>"},{"location":"api-reference/backup/#seadex.SeaDexBackup.delete","title":"delete","text":"<pre><code>delete(file: str | BackupFile) -&gt; None\n</code></pre> <p>Delete the specified backup file.</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>str | BackupFile</code> <p>The backup file to delete.</p> required <p>Returns:</p> Type Description <code>None</code> Source code in <code>src/seadex/_backup.py</code> <pre><code>def delete(self, file: str | BackupFile) -&gt; None:\n    \"\"\"\n    Delete the specified backup file.\n\n    Parameters\n    ----------\n    file : str | BackupFile\n        The backup file to delete.\n\n    Returns\n    -------\n    None\n\n    \"\"\"\n    self._client.delete(\n        self._url_for(f\"/api/backups/{file}\"), headers={\"Authorization\": self._admin_token}\n    ).raise_for_status()\n</code></pre>"},{"location":"api-reference/backup/#seadex.SeaDexBackup.download","title":"download","text":"<pre><code>download(file: str | BackupFile | None = None, *, destination: StrPath = Path.cwd()) -&gt; Path\n</code></pre> <p>Download the specified backup file to the given destination directory.</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>str | BackupFile | None</code> <p>The backup file to download. If <code>None</code>, downloads the latest existing backup.</p> <code>None</code> <code>destination</code> <code>StrPath</code> <p>The destination directory to save the backup.</p> <code>cwd()</code> <p>Returns:</p> Type Description <code>Path</code> <p>The path to the downloaded backup file.</p> <p>Raises:</p> Type Description <code>NotADirectoryError</code> <p>If the destination is not a valid directory.</p> <code>BadBackupFileError</code> <p>if the downloaded backup file fails integrity check.</p> Source code in <code>src/seadex/_backup.py</code> <pre><code>def download(self, file: str | BackupFile | None = None, *, destination: StrPath = Path.cwd()) -&gt; Path:\n    \"\"\"\n    Download the specified backup file to the given destination directory.\n\n    Parameters\n    ----------\n    file : str | BackupFile | None, optional\n        The backup file to download. If `None`, downloads the [latest existing backup][seadex.SeaDexBackup.latest_backup].\n    destination : StrPath, optional\n        The destination directory to save the backup.\n\n    Returns\n    -------\n    Path\n        The path to the downloaded backup file.\n\n    Raises\n    ------\n    NotADirectoryError\n        If the destination is not a valid directory.\n    BadBackupFileError\n        if the downloaded backup file fails integrity check.\n\n    \"\"\"\n    destination = realpath(destination)\n\n    if not destination.is_dir():\n        raise NotADirectoryError(f\"{destination} must be an existing directory!\")\n\n    match file:\n        case None:\n            key = self.latest_backup.name\n        case str():\n            key = file\n        case BackupFile():\n            key = file.name\n        case _:\n            assert_never(file)\n\n    outfile = destination / key\n\n    with TemporaryDirectory(ignore_cleanup_errors=True) as tmpdir:\n        tmpfile = Path(tmpdir).resolve() / str(uuid4())\n        response = self._client.get(self._url_for(f\"/api/backups/{key}\"), params={\"token\": self._get_file_token()})\n        tmpfile.write_bytes(response.content)\n        shutil.move(tmpfile, outfile)\n\n    with ZipFile(outfile) as archive:\n        check = archive.testzip()\n\n    if check is not None:\n        outfile.unlink(missing_ok=True)\n        raise BadBackupFileError(f\"{outfile} failed integrity check!\")\n\n    return outfile\n</code></pre>"},{"location":"api-reference/backup/#seadex.BackupFile","title":"BackupFile","text":"<p>               Bases: <code>FrozenBaseModel</code></p> <p>A model representing a backup file.</p>"},{"location":"api-reference/backup/#seadex.BackupFile.modified_time","title":"modified_time  <code>instance-attribute</code>","text":"<pre><code>modified_time: UTCDateTime\n</code></pre> <p>The last modified time of the backup file.</p>"},{"location":"api-reference/backup/#seadex.BackupFile.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>The name of the backup file.</p>"},{"location":"api-reference/backup/#seadex.BackupFile.size","title":"size  <code>instance-attribute</code>","text":"<pre><code>size: ByteSize\n</code></pre> <p>The size of the backup file in bytes.</p>"},{"location":"api-reference/entry/","title":"Entry","text":""},{"location":"api-reference/entry/#seadex.SeaDexEntry","title":"SeaDexEntry","text":"<pre><code>SeaDexEntry(base_url: str = 'https://releases.moe', client: Client | None = None)\n</code></pre> <p>Client to interact with the anime entries in SeaDex.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>The base URL of SeaDex, used for constructing API queries.</p> <code>'https://releases.moe'</code> <code>client</code> <code>Client</code> <p>An <code>httpx.Client</code> instance used to make requests to SeaDex.</p> <code>None</code> <p>Examples:</p> <pre><code>with SeaDexEntry() as entry:\n    tamako = entry.from_title(\"tamako love story\")\n    for torrent in tamako.torrents:\n        if torrent.is_best and torrent.tracker.is_public():\n            print(torrent.release_group)\n            #&gt; LYS1TH3A\n            #&gt; Okay-Subs\n</code></pre> Source code in <code>src/seadex/_entry.py</code> <pre><code>def __init__(self, base_url: str = \"https://releases.moe\", client: Client | None = None) -&gt; None:\n    \"\"\"\n    Client to interact with the anime entries in SeaDex.\n\n    Parameters\n    ----------\n    base_url : str, optional\n        The base URL of SeaDex, used for constructing API queries.\n    client : Client, optional\n        An [`httpx.Client`](https://www.python-httpx.org/api/#client) instance used to make requests to SeaDex.\n\n    Examples\n    --------\n    ```py\n    with SeaDexEntry() as entry:\n        tamako = entry.from_title(\"tamako love story\")\n        for torrent in tamako.torrents:\n            if torrent.is_best and torrent.tracker.is_public():\n                print(torrent.release_group)\n                #&gt; LYS1TH3A\n                #&gt; Okay-Subs\n    ```\n\n    \"\"\"\n    self._base_url = base_url\n    self._endpoint = urljoin(self._base_url, \"/api/collections/entries/records\")\n    self._client = httpx_client() if client is None else client\n</code></pre>"},{"location":"api-reference/entry/#seadex.SeaDexEntry.base_url","title":"base_url  <code>property</code>","text":"<pre><code>base_url: str\n</code></pre> <p>Base URL, used for constructing API queries.</p>"},{"location":"api-reference/entry/#seadex.SeaDexEntry.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close the underlying HTTP client connection.</p> Source code in <code>src/seadex/_entry.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"\n    Close the underlying HTTP client connection.\n    \"\"\"\n    self._client.close()\n</code></pre>"},{"location":"api-reference/entry/#seadex.SeaDexEntry.from_filename","title":"from_filename","text":"<pre><code>from_filename(filename: StrPath) -&gt; Iterator[EntryRecord]\n</code></pre> <p>Yield entries that may contain a file with the specified filename.</p> <p>Parameters:</p> Name Type Description Default <code>filename</code> <code>StrPath</code> <p>The filename to search for.</p> required <p>Yields:</p> Type Description <code>EntryRecord</code> <p>The retrieved entry.</p> Source code in <code>src/seadex/_entry.py</code> <pre><code>def from_filename(self, filename: StrPath, /) -&gt; Iterator[EntryRecord]:\n    \"\"\"\n    Yield entries that may contain a file with the specified filename.\n\n    Parameters\n    ----------\n    filename : StrPath\n        The filename to search for.\n\n    Yields\n    ------\n    EntryRecord\n        The retrieved entry.\n\n    \"\"\"\n    basefilename = basename(filename)\n    params = {\"filter\": f'trs.files?~\\'\"name\":\"{basefilename}\"\\'', \"expand\": \"trs\"}\n    response = self._client.get(self._endpoint, params=params).raise_for_status()\n    for entry in response.json()[\"items\"]:\n        yield EntryRecord._from_dict(entry)\n</code></pre>"},{"location":"api-reference/entry/#seadex.SeaDexEntry.from_id","title":"from_id","text":"<pre><code>from_id(id: int | str) -&gt; EntryRecord\n</code></pre> <p>Retrieve an entry by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>int | str</code> <p>The ID of the entry. Can be an AniList ID (integer) or a SeaDex database ID (string).</p> required <p>Returns:</p> Type Description <code>EntryRecord</code> <p>The retrieved entry.</p> <p>Raises:</p> Type Description <code>EntryNotFoundError</code> <p>If no entry is found for the provided ID.</p> Source code in <code>src/seadex/_entry.py</code> <pre><code>def from_id(self, id: int | str, /) -&gt; EntryRecord:\n    \"\"\"\n    Retrieve an entry by its ID.\n\n    Parameters\n    ----------\n    id : int | str\n        The ID of the entry. Can be an AniList ID (integer)\n        or a SeaDex database ID (string).\n\n    Returns\n    -------\n    EntryRecord\n        The retrieved entry.\n\n    Raises\n    ------\n    EntryNotFoundError\n        If no entry is found for the provided ID.\n\n    \"\"\"\n    try:\n        if isinstance(id, int):\n            params = {\"filter\": f\"alID={id}\", \"expand\": \"trs\"}  # Anilist IDs are integers\n        else:\n            params = {\"filter\": f\"id='{id}'\", \"expand\": \"trs\"}  # Database IDs are strings\n\n        response = self._client.get(self._endpoint, params=params).raise_for_status()\n        return EntryRecord._from_dict(response.json()[\"items\"][0])\n\n    except (KeyError, IndexError):\n        errmsg = f\"No seadex entry found for id: {id}\"\n        raise EntryNotFoundError(errmsg, response=response)\n</code></pre>"},{"location":"api-reference/entry/#seadex.SeaDexEntry.from_title","title":"from_title","text":"<pre><code>from_title(title: str) -&gt; EntryRecord\n</code></pre> <p>Retrieve an entry by its anime title.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>The title of the anime to search for.</p> required <p>Returns:</p> Type Description <code>EntryRecord</code> <p>The retrieved entry.</p> <p>Raises:</p> Type Description <code>EntryNotFoundError</code> <p>If no entry is found for the provided title.</p> Source code in <code>src/seadex/_entry.py</code> <pre><code>def from_title(self, title: str, /) -&gt; EntryRecord:\n    \"\"\"\n    Retrieve an entry by its anime title.\n\n    Parameters\n    ----------\n    title : str\n        The title of the anime to search for.\n\n    Returns\n    -------\n    EntryRecord\n        The retrieved entry.\n\n    Raises\n    ------\n    EntryNotFoundError\n        If no entry is found for the provided title.\n\n    \"\"\"\n    try:\n        response = self._client.post(\n            \"https://graphql.anilist.co\",\n            json={\n                \"query\": \"query ($search: String!) { Media(search: $search, type: ANIME) { id title { english romaji } } }\",\n                \"variables\": {\"search\": title},\n            },\n        )\n        media = response.json()[\"data\"][\"Media\"]\n        anilist_id = media[\"id\"]\n        response = self._client.get(\n            self._endpoint, params={\"filter\": f\"alID={anilist_id}\", \"expand\": \"trs\"}\n        ).raise_for_status()\n        entry_record = EntryRecord._from_dict(response.json()[\"items\"][0])\n        setattr(entry_record, \"__anilist_title\", media[\"title\"][\"english\"] or media[\"title\"][\"romaji\"])\n        return entry_record\n\n    except (KeyError, IndexError, TypeError):\n        errmsg = f\"No seadex entry found for title: {title}\"\n        raise EntryNotFoundError(errmsg, response=response)\n</code></pre>"},{"location":"api-reference/entry/#seadex.SeaDexEntry.iterator","title":"iterator","text":"<pre><code>iterator() -&gt; Iterator[EntryRecord]\n</code></pre> <p>Lazily get all the entries from SeaDex.</p> <p>Yields:</p> Type Description <code>EntryRecord</code> <p>The retrieved entry.</p> Source code in <code>src/seadex/_entry.py</code> <pre><code>def iterator(self) -&gt; Iterator[EntryRecord]:\n    \"\"\"\n    Lazily get all the entries from SeaDex.\n\n    Yields\n    ------\n    EntryRecord\n        The retrieved entry.\n\n    \"\"\"\n    total_pages = (\n        self._client.get(self._endpoint, params={\"perPage\": 500}).raise_for_status().json()[\"totalPages\"] + 1\n    )\n\n    for page in range(1, total_pages):\n        response = self._client.get(\n            self._endpoint, params={\"page\": page, \"perPage\": 500, \"expand\": \"trs\"}\n        ).raise_for_status()\n        for item in response.json()[\"items\"]:\n            yield EntryRecord._from_dict(item)\n</code></pre>"},{"location":"api-reference/enums/","title":"Enums","text":""},{"location":"api-reference/enums/#seadex.Tracker","title":"Tracker","text":"<p>               Bases: <code>CaseInsensitiveStrEnum</code></p> <p>Enum of public and private trackers.</p>"},{"location":"api-reference/enums/#seadex.Tracker.AITHER","title":"AITHER  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>AITHER = 'Aither'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.ANIDEX","title":"ANIDEX  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ANIDEX = 'AniDex'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.ANIMEBYTES","title":"ANIMEBYTES  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ANIMEBYTES = 'AnimeBytes'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.ANIMETOSHO","title":"ANIMETOSHO  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ANIMETOSHO = 'AnimeTosho'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.BEYONDHD","title":"BEYONDHD  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BEYONDHD = 'BeyondHD'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.BLUTOPIA","title":"BLUTOPIA  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BLUTOPIA = 'Blutopia'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.BROADCASTTHENET","title":"BROADCASTTHENET  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BROADCASTTHENET = 'BroadcastTheNet'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.HDBITS","title":"HDBITS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>HDBITS = 'HDBits'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.NYAA","title":"NYAA  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>NYAA = 'Nyaa'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.OTHER","title":"OTHER  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>OTHER = 'Other'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.PASSTHEPOPCORN","title":"PASSTHEPOPCORN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PASSTHEPOPCORN = 'PassThePopcorn'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.RUTRACKER","title":"RUTRACKER  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>RUTRACKER = 'RuTracker'\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.domain","title":"domain  <code>property</code>","text":"<pre><code>domain: str\n</code></pre> <p>Returns the domain name associated with the current tracker.</p> <p>Returns:</p> Type Description <code>str</code> <p>Domain name of the tracker, or an empty string for \"Other\".</p>"},{"location":"api-reference/enums/#seadex.Tracker.is_private","title":"is_private","text":"<pre><code>is_private() -&gt; bool\n</code></pre> <p>Check if the current tracker is private.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the tracker is private, <code>False</code> otherwise.</p> Source code in <code>src/seadex/_enums.py</code> <pre><code>def is_private(self) -&gt; bool:\n    \"\"\"\n    Check if the current tracker is private.\n\n    Returns\n    -------\n    bool\n        `True` if the tracker is private, `False` otherwise.\n\n    \"\"\"\n    return False if self.value in (\"Nyaa\", \"AnimeTosho\", \"AniDex\", \"RuTracker\") else True\n</code></pre>"},{"location":"api-reference/enums/#seadex.Tracker.is_public","title":"is_public","text":"<pre><code>is_public() -&gt; bool\n</code></pre> <p>Check if the current tracker is public.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if the tracker is public, <code>False</code> otherwise.</p> Source code in <code>src/seadex/_enums.py</code> <pre><code>def is_public(self) -&gt; bool:\n    \"\"\"\n    Check if the current tracker is public.\n\n    Returns\n    -------\n    bool\n        `True` if the tracker is public, `False` otherwise.\n\n    \"\"\"\n    return not self.is_private()\n</code></pre>"},{"location":"api-reference/exceptions/","title":"Exceptions","text":""},{"location":"api-reference/exceptions/#seadex.SeaDexError","title":"SeaDexError","text":"<pre><code>SeaDexError(message: str)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>Base Exception for all SeaDex related errors.</p> Source code in <code>src/seadex/_exceptions.py</code> <pre><code>def __init__(self, message: str) -&gt; None:\n    self.message = message\n    super().__init__(message)\n</code></pre>"},{"location":"api-reference/exceptions/#seadex.EntryNotFoundError","title":"EntryNotFoundError","text":"<pre><code>EntryNotFoundError(message: str, *, response: Response)\n</code></pre> <p>               Bases: <code>SeaDexError</code></p> <p>The requested Entry was not found in SeaDex.</p> Source code in <code>src/seadex/_exceptions.py</code> <pre><code>def __init__(self, message: str, *, response: Response) -&gt; None:\n    self.response = response\n    super().__init__(message)\n</code></pre>"},{"location":"api-reference/exceptions/#seadex.BadBackupFileError","title":"BadBackupFileError","text":"<pre><code>BadBackupFileError(message: str)\n</code></pre> <p>               Bases: <code>SeaDexError</code></p> <p>The backup file is broken.</p> Source code in <code>src/seadex/_exceptions.py</code> <pre><code>def __init__(self, message: str) -&gt; None:\n    self.message = message\n    super().__init__(message)\n</code></pre>"},{"location":"api-reference/records/","title":"Records","text":""},{"location":"api-reference/records/#seadex.EntryRecord","title":"EntryRecord","text":"<p>               Bases: <code>FrozenBaseModel</code></p> <p>Represents a single anime entry in SeaDex.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.anilist_id","title":"anilist_id  <code>instance-attribute</code>","text":"<pre><code>anilist_id: int\n</code></pre> <p>The AniList ID of the anime.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.collection_id","title":"collection_id  <code>instance-attribute</code>","text":"<pre><code>collection_id: str\n</code></pre> <p>The ID of the collection the entry belongs to.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.collection_name","title":"collection_name  <code>instance-attribute</code>","text":"<pre><code>collection_name: str\n</code></pre> <p>The name of the collection the entry belongs to.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.comparisons","title":"comparisons  <code>instance-attribute</code>","text":"<pre><code>comparisons: tuple[str, ...]\n</code></pre> <p>A tuple of comparison urls.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.created_at","title":"created_at  <code>instance-attribute</code>","text":"<pre><code>created_at: UTCDateTime\n</code></pre> <p>The timestamp of when the entry was created.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.id","title":"id  <code>instance-attribute</code>","text":"<pre><code>id: str\n</code></pre> <p>The ID of the entry.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.is_incomplete","title":"is_incomplete  <code>instance-attribute</code>","text":"<pre><code>is_incomplete: bool\n</code></pre> <p>Whether the entry is considered incomplete.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.notes","title":"notes  <code>instance-attribute</code>","text":"<pre><code>notes: str\n</code></pre> <p>Additional notes about the entry.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.theoretical_best","title":"theoretical_best  <code>instance-attribute</code>","text":"<pre><code>theoretical_best: str | None\n</code></pre> <p>The theoretical best release for the entry, if known.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.torrents","title":"torrents  <code>instance-attribute</code>","text":"<pre><code>torrents: tuple[TorrentRecord, ...]\n</code></pre> <p>A tuple of <code>TorrentRecord</code> objects associated with the entry.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.updated_at","title":"updated_at  <code>instance-attribute</code>","text":"<pre><code>updated_at: UTCDateTime\n</code></pre> <p>The timestamp of when the entry was last updated.</p>"},{"location":"api-reference/records/#seadex.EntryRecord.url","title":"url  <code>property</code>","text":"<pre><code>url: str\n</code></pre> <p>The URL of the entry.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord","title":"TorrentRecord","text":"<p>               Bases: <code>FrozenBaseModel</code></p> <p>Represents a single torrent record within a SeaDex entry.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.collection_id","title":"collection_id  <code>instance-attribute</code>","text":"<pre><code>collection_id: str\n</code></pre> <p>The ID of the collection the torrent record belongs to.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.collection_name","title":"collection_name  <code>instance-attribute</code>","text":"<pre><code>collection_name: str\n</code></pre> <p>The name of the collection the torrent record belongs to.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.created_at","title":"created_at  <code>instance-attribute</code>","text":"<pre><code>created_at: UTCDateTime\n</code></pre> <p>The timestamp of when the torrent record was created.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.files","title":"files  <code>instance-attribute</code>","text":"<pre><code>files: tuple[File, ...]\n</code></pre> <p>A tuple of <code>File</code> objects representing the files in the torrent.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.id","title":"id  <code>instance-attribute</code>","text":"<pre><code>id: str\n</code></pre> <p>The ID of the torrent record.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.infohash","title":"infohash  <code>instance-attribute</code>","text":"<pre><code>infohash: str | None\n</code></pre> <p>The infohash of the torrent if available, otherwise <code>None</code> (private torrents).</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.is_best","title":"is_best  <code>instance-attribute</code>","text":"<pre><code>is_best: bool\n</code></pre> <p>Whether this torrent is marked as the \"best\".</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.is_dual_audio","title":"is_dual_audio  <code>instance-attribute</code>","text":"<pre><code>is_dual_audio: bool\n</code></pre> <p>Whether the torrent contains both Japanese and English audio tracks.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.release_group","title":"release_group  <code>instance-attribute</code>","text":"<pre><code>release_group: str\n</code></pre> <p>The name of the group that released the torrent.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.tracker","title":"tracker  <code>instance-attribute</code>","text":"<pre><code>tracker: Tracker\n</code></pre> <p>The tracker where the torrent is hosted.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.updated_at","title":"updated_at  <code>instance-attribute</code>","text":"<pre><code>updated_at: UTCDateTime\n</code></pre> <p>The timestamp of when the torrent record was last updated.</p>"},{"location":"api-reference/records/#seadex.TorrentRecord.url","title":"url  <code>instance-attribute</code>","text":"<pre><code>url: str\n</code></pre> <p>The URL of the torrent.</p>"},{"location":"api-reference/torrent/","title":"Torrent","text":""},{"location":"api-reference/torrent/#seadex.SeaDexTorrent","title":"SeaDexTorrent","text":"<pre><code>SeaDexTorrent(file: StrPath)\n</code></pre> <p>Class to handle torrent files for SeaDex.</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>StrPath</code> <p>The path to the torrent file.</p> required Source code in <code>src/seadex/_torrent.py</code> <pre><code>def __init__(self, file: StrPath) -&gt; None:\n    \"\"\"\n    Class to handle torrent files for SeaDex.\n\n    Parameters\n    ----------\n    file : StrPath\n        The path to the torrent file.\n\n    \"\"\"\n    self._file = realpath(file)\n</code></pre>"},{"location":"api-reference/torrent/#seadex.SeaDexTorrent.file","title":"file  <code>cached</code> <code>property</code>","text":"<pre><code>file: Path\n</code></pre> <p>Resolved path to the torrent file.</p>"},{"location":"api-reference/torrent/#seadex.SeaDexTorrent.filelist","title":"filelist  <code>cached</code> <code>property</code>","text":"<pre><code>filelist: FileList\n</code></pre> <p>List of files within the torrent.</p>"},{"location":"api-reference/torrent/#seadex.SeaDexTorrent.sanitize","title":"sanitize","text":"<pre><code>sanitize(*, destination: StrPath | None = None, overwrite: bool = False) -&gt; Path\n</code></pre> <p>Sanitizes the torrent file by removing sensitive data and optionally saves it to a new location.</p> <p>Parameters:</p> Name Type Description Default <code>destination</code> <code>StrPath | None</code> <p>The destination path to save the sanitized torrent. If None, the sanitized file is saved in place.</p> <code>None</code> <code>overwrite</code> <code>bool</code> <p>If True, overwrites the existing file or destination file if it exists.</p> <code>False</code> <p>Returns:</p> Type Description <code>Path</code> <p>The path to the sanitized torrent file.</p> <p>Raises:</p> Type Description <code>FileExistsError</code> <ul> <li>If <code>destination</code> is None and <code>overwrite</code> is False.</li> <li>If <code>destination</code> already exists and <code>overwrite</code> is False.</li> </ul> Notes <ul> <li>If the torrent file is public (i.e., not marked as private), it is returned as is.</li> <li>The following fields are removed from the torrent file if it is private:<ul> <li>Trackers</li> <li>Web seeds</li> <li>HTTP seeds</li> <li>Private flag</li> <li>Comment</li> <li>Creation date</li> <li>Created by field</li> <li>Source field</li> </ul> </li> <li>The torrent's <code>infohash</code> is randomized.</li> </ul> Source code in <code>src/seadex/_torrent.py</code> <pre><code>def sanitize(self, *, destination: StrPath | None = None, overwrite: bool = False) -&gt; Path:\n    \"\"\"\n    Sanitizes the torrent file by removing sensitive data and optionally saves it to a new location.\n\n    Parameters\n    ----------\n    destination : StrPath | None, optional\n        The destination path to save the sanitized torrent. If None, the sanitized file is saved in place.\n    overwrite : bool, optional\n        If True, overwrites the existing file or destination file if it exists.\n\n    Returns\n    -------\n    Path\n        The path to the sanitized torrent file.\n\n    Raises\n    ------\n    FileExistsError\n        - If `destination` is None and `overwrite` is False.\n        - If `destination` already exists and `overwrite` is False.\n\n    Notes\n    -----\n    - If the torrent file is public (i.e., not marked as private), it is returned as is.\n    - The following fields are removed from the torrent file if it is private:\n        - Trackers\n        - Web seeds\n        - HTTP seeds\n        - Private flag\n        - Comment\n        - Creation date\n        - Created by field\n        - Source field\n    - The torrent's `infohash` is randomized.\n\n    \"\"\"\n    torrent = TorfTorrent.read(self.file)\n\n    if not torrent.private:\n        # Public torrent\n        return self.file\n\n    torrent.trackers = None\n    torrent.webseeds = None\n    torrent.httpseeds = None\n    torrent.private = None\n    torrent.comment = None\n    torrent.creation_date = None\n    torrent.created_by = None\n    torrent.source = None\n    torrent.randomize_infohash = True\n\n    if destination is None:\n        if overwrite is False:\n            raise FileExistsError(f\"{self.file} already exists!\")\n        else:\n            torrent.write(self.file, overwrite=True)\n            return self.file\n    else:\n        destination = realpath(destination)\n        torrent.write(destination, overwrite=overwrite)\n        return destination\n</code></pre>"},{"location":"api-reference/torrent/#seadex.FileList","title":"FileList","text":"<p>               Bases: <code>tuple[File, ...]</code></p> <p>A tuple-based collection of <code>File</code> objects.</p>"},{"location":"api-reference/torrent/#seadex.FileList.to_json","title":"to_json","text":"<pre><code>to_json(indent: int | None = None) -&gt; str\n</code></pre> <p>Convert the file list to a JSON string that's compatible with SeaDex.</p> <p>Parameters:</p> Name Type Description Default <code>indent</code> <code>int | None</code> <p>Number of spaces for indentation in the output JSON string.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>The file list in JSON format.</p> Source code in <code>src/seadex/_torrent.py</code> <pre><code>def to_json(self, indent: int | None = None) -&gt; str:\n    \"\"\"\n    Convert the file list to a JSON string that's compatible with SeaDex.\n\n    Parameters\n    ----------\n    indent : int | None, optional\n        Number of spaces for indentation in the output JSON string.\n\n    Returns\n    -------\n    str\n        The file list in JSON format.\n\n    \"\"\"\n    files = []\n\n    for file in self:\n        files.append({\"filename\": basename(file.name), \"size\": int(file.size)})\n    return json.dumps(files, indent=indent)\n</code></pre>"},{"location":"api-reference/torrent/#seadex.File","title":"File","text":"<p>               Bases: <code>FrozenBaseModel</code></p> <p>Represents a file in the torrent.</p>"},{"location":"api-reference/torrent/#seadex.File.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>The name of the file.</p>"},{"location":"api-reference/torrent/#seadex.File.size","title":"size  <code>instance-attribute</code>","text":"<pre><code>size: ByteSize\n</code></pre> <p>The size of the file in bytes.</p>"},{"location":"api-reference/types/","title":"Types","text":""},{"location":"api-reference/types/#seadex._types.UTCDateTime","title":"UTCDateTime  <code>module-attribute</code>","text":"<pre><code>UTCDateTime: TypeAlias = Annotated[datetime, AfterValidator(lambda dt: astimezone(utc))]\n</code></pre> <p>Datetime that's always in UTC.</p>"},{"location":"api-reference/types/#seadex._types.StrPath","title":"StrPath  <code>module-attribute</code>","text":"<pre><code>StrPath: TypeAlias = str | PathLike[str]\n</code></pre> <p>String or path-like objects</p>"}]}